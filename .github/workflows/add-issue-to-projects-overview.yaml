on:
  issues:
    types:
      - opened

jobs:
  add-to-projects-overview:
    uses: NaaVRE/test-add-to-project2/.github/workflows/add-issue-to-projects-overview.yaml@main
    secrets: 
      personal_access_token: ${{ secrets.ADD_TO_PROJECT_PAT }}
